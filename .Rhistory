reframe(median_reads_in = median(reads.in),
median_reads_out = median(reads.out),
median_percent_retained = (median(reads.out)/median(reads.in)),
max_percent_retained = max(reads.out/reads.in),
min_percent_retained = min(reads.out/reads.in))
# Take a look at it!
read_stats_df
# Plot it
numSeqs_QC_dotplot <-
filtered_df %>%
ggplot(aes(x = reads.in, y = reads.out)) +
geom_point(alpha = 0.5, size = 2) +
labs(x = "# of Raw Seqs",
y = "# of Seqs Retained") +
# Now let's add a 1:1 line for reference of keeping 100% of the reads
geom_abline(slope=1, intercept = 0, color = "deeppink")
library(ggplot2)
# Plot it
numSeqs_QC_dotplot <-
filtered_df %>%
ggplot(aes(x = reads.in, y = reads.out)) +
geom_point(alpha = 0.5, size = 2) +
labs(x = "# of Raw Seqs",
y = "# of Seqs Retained") +
# Now let's add a 1:1 line for reference of keeping 100% of the reads
geom_abline(slope=1, intercept = 0, color = "deeppink")
#View Plot
numSeqs_QC_dotplot
# Now, let's look at the number of reads retained in a histogram
numRetained_QC_histplot <-
filtered_df %>%
ggplot(aes(x = reads.out)) +
geom_histogram() +
labs(x = "# of Seqs Retained",
y = "# of Samples")
# Create a histogram of percent reads retained in a histogram
percSeqs_QC_histplot <-
filtered_df %>%
ggplot(aes(x = percent.retained)) +
geom_histogram() +
labs(x = "% of Seqs Retained",
y = "# of Samples") +
# Set the scale to be between 0-1 (0-100%)
scale_x_continuous(limits = c(0, 1))
#View histogram
percSeqs_QC_histplot
# Now, let's put the plots together
numSeqs_QC_dotplot + numRetained_QC_histplot + percSeqs_QC_histplot +
plot_annotation(tag_levels = 'A')
library(patchwork)
# Now, let's put the plots together
numSeqs_QC_dotplot + numRetained_QC_histplot + percSeqs_QC_histplot +
plot_annotation(tag_levels = 'A')
read_stats_df$max_percent_retained
read_stats_df$min_percent_retained
read_stats_df$median_percent_retained
#View Plot
numRetained_QC_histplot
numRetained_QC_histplot_edit <-
filtered_df %>%
ggplot(aes(x = reads.out)) +
geom_histogram() +
labs(x = "# of Seqs Retained",
y = "# of Samples") +
scale_x_continuous(labels = comma) +
scale_y_continuous(labels = comma)  # Formats axis labels with commas
library(scales)
numRetained_QC_histplot_edit <-
filtered_df %>%
ggplot(aes(x = reads.out)) +
geom_histogram() +
labs(x = "# of Seqs Retained",
y = "# of Samples") +
scale_x_continuous(labels = comma) +
scale_y_continuous(labels = comma)  # Formats axis labels with commas
numRetained_QC_histplot_edit
numRetained_QC_histplot_edit <-
filtered_df %>%
ggplot(aes(x = reads.out)) +
geom_histogram(bins = 50) +  # Set the number of bins you want
labs(x = "# of Seqs Retained",
y = "# of Samples") +
scale_x_continuous(labels = comma) +
scale_y_continuous(labels = comma)
numRetained_QC_histplot_edit <-
filtered_df %>%
ggplot(aes(x = reads.out)) +
geom_histogram(binwidth = 1000) +  # Adjust bin width as needed
labs(x = "# of Seqs Retained",
y = "# of Samples") +
scale_x_continuous(labels = comma) +
scale_y_continuous(labels = comma)
numRetained_QC_histplot_edit
numRetained_QC_histplot_edit <-
filtered_df %>%
ggplot(aes(x = reads.out)) +
geom_histogram(bins = 50) +  # Set the number of bins you want
labs(x = "# of Seqs Retained",
y = "# of Samples") +
scale_x_continuous(labels = comma) +
scale_y_continuous(labels = comma)
numRetained_QC_histplot_edit
# Plot the pre and post together in one plot
preQC_aggregate_plot / postQC_aggregate_plot
BiocManager::install("ShortRead")
# Plot the pre and post together in one plot
preQC_aggregate_plot / postQC_aggregate_plot
---
title: "Assigning ASVs with DADA2"
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
fig.align = "center",
# Send figures generated in this file to this folder below
fig.path = "../figures/02_AssignASVs/")
# Set the seed for reproducibility
set.seed(111111)
# Let's make a parameter to set the number of threads
n_threads = 20
# What time did we start running this script?
start_time <- Sys.time()
# Efficient package loading with pacman
pacman::p_load(tidyverse, devtools, dada2,
patchwork, DT, install = FALSE)
# Place filtered seq files into a variable
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition check:
filtered_fastqs_path
# Intuition check:
filtered_fastqs_path
# Create Forward vector
filtered_forward_reads <-
list.files(filtered_fastqs_path, pattern = "1_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_forward_reads[1:5]
# Create Forward vector
filtered_forward_reads <-
list.files(filtered_fastqs_path, pattern = "_1_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_forward_reads[1:5]
# Reverse vector
filtered_reverse_reads <-
list.files(filtered_fastqs_path, pattern = "2_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_reverse_reads[1:5]
# Place filtered seq files into a variable
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition check:
filtered_fastqs_path
View(forward_filteredQual_plot_12)
View(filtered_reads)
# Place filtered seq files into a variable
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition check:
filtered_fastqs_path
# Create Forward vector
filtered_forward_reads <-
list.files(filtered_fastqs_path, pattern = "1_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_forward_reads[1:5]
# Reverse vector
filtered_reverse_reads <-
list.files(filtered_fastqs_path, pattern = "2_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_reverse_reads[1:5]
# Create vector of sample names from the filenames
sample_names <- sapply(strsplit(basename(filtered_forward_reads), "_"), `[`,1)
# Intuition Check
head(sample_names)
# Place filtered reads into filtered_fastqs_path
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition Check
filtered_fastqs_path
# create 2 vectors: filtered_forward_reads & filtered_reverse_reads
filtered_forward_reads <-
file.path(filtered_fastqs_path, paste0(sample_names, "_1.fastq.gz"))
# create 2 vectors: filtered_forward_reads & filtered_reverse_reads
filtered_forward_reads <-
file.path(filtered_fastqs_path, paste0(sample_names, "_1.fastq.gz"))
# Intuition Check
length(filtered_forward_reads)
# reverse reads
filtered_reverse_reads <-
file.path(filtered_fastqs_path, paste0(sample_names, "_2.fastq.gz"))
# Intuition Check
head(filtered_reverse_reads)
# Intuition Check
head(filtered_reverse_reads)
# Intuition Check #2
length(filtered_reverse_reads)
# Chunk 1: setup
knitr::opts_chunk$set(echo = TRUE,
fig.align = "center",
# Send figures generated in this file to this folder below
fig.path = "../figures/02_AssignASVs/")
# Chunk 2: set-seed
# Set the seed for reproducibility
set.seed(111111)
# Let's make a parameter to set the number of threads
n_threads = 20
# Chunk 3: rmd-start
# What time did we start running this script?
start_time <- Sys.time()
# Chunk 4: load-packages
# Efficient package loading with pacman
pacman::p_load(tidyverse, devtools, dada2,
patchwork, DT, install = FALSE)
# Chunk 5: load-filtered-fastqs
# Place filtered seq files into a variable
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition check:
filtered_fastqs_path
# Create Forward vector
filtered_forward_reads <-
list.files(filtered_fastqs_path, pattern = "_1_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_forward_reads[1:5]
# Reverse vector
filtered_reverse_reads <-
list.files(filtered_fastqs_path, pattern = "_2_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_reverse_reads[1:5]
# Chunk 6: sample-names
# Create vector of sample names from the filenames
sample_names <- sapply(strsplit(basename(filtered_forward_reads), "_"), `[`,1)
# Intuition Check
head(sample_names)
# Chunk 7: learn-errors-MiSeq
# Forward Reads
error_forward_reads <-
learnErrors(filtered_forward_reads, multithread = n_threads)
# Place filtered seq files into a variable
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition check:
filtered_fastqs_path
# Create Forward vector
filtered_forward_reads <-
list.files(filtered_fastqs_path, pattern = "1_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_forward_reads[1:5]
# Reverse vector
filtered_reverse_reads <-
list.files(filtered_fastqs_path, pattern = "2_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_reverse_reads[1:5]
# Create vector of sample names from the filenames
sample_names <- sapply(strsplit(basename(filtered_forward_reads), "_"), `[`,1)
# Intuition Check
head(sample_names)
# Forward Reads
error_forward_reads <-
learnErrors(filtered_forward_reads, multithread = n_threads)
# Forward Error Plot
forward_error_plot <-
plotErrors(error_forward_reads, nominalQ = TRUE) +
labs(title = "Forward Reads: Error Model")
# Reverse Reads
error_reverse_reads <-
learnErrors(filtered_reverse_reads, multithread = n_threads)
# Reverse Error Plot
reverse_error_plot <-
plotErrors(error_reverse_reads, nominalQ = TRUE) +
labs(title = "Reverse Reads: Error Model")
# Look at the plots together
forward_error_plot + reverse_error_plot
merged_ASVs <-
mergePairs(dada_forward, filtered_forward_reads,
dada_reverse, filtered_reverse_reads,
verbose = TRUE)
# Infer ASVs on the forward sequences
dada_forward <-
dada(filtered_forward_reads,
err = error_forward_reads,
multithread = n_threads)
# Chunk 1: setup
knitr::opts_chunk$set(echo = TRUE,
fig.align = "center",
# Send figures generated in this file to this folder below
fig.path = "../figures/02_AssignASVs/")
# Chunk 2: set-seed
# Set the seed for reproducibility
set.seed(111111)
# Let's make a parameter to set the number of threads
n_threads = 20
# Chunk 3: rmd-start
# What time did we start running this script?
start_time <- Sys.time()
# Chunk 4: load-packages
# Efficient package loading with pacman
pacman::p_load(tidyverse, devtools, dada2,
patchwork, DT, install = FALSE)
# Chunk 5: load-filtered-fastqs
# Place filtered seq files into a variable
filtered_fastqs_path <- "data/01_DADA2/02_filtered_fastqs"
# Intuition check:
filtered_fastqs_path
# Create Forward vector
filtered_forward_reads <-
list.files(filtered_fastqs_path, pattern = "1_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_forward_reads[1:5]
# Reverse vector
filtered_reverse_reads <-
list.files(filtered_fastqs_path, pattern = "2_filtered.fastq.gz",
full.names = TRUE)
# Check
filtered_reverse_reads[1:5]
# Chunk 6: sample-names
# Create vector of sample names from the filenames
sample_names <- sapply(strsplit(basename(filtered_forward_reads), "_"), `[`,1)
# Intuition Check
head(sample_names)
# Chunk 7: learn-errors-MiSeq
# Forward Reads
error_forward_reads <-
learnErrors(filtered_forward_reads, multithread = n_threads)
# Forward Error Plot
forward_error_plot <-
plotErrors(error_forward_reads, nominalQ = TRUE) +
labs(title = "Forward Reads: Error Model")
# Reverse Reads
error_reverse_reads <-
learnErrors(filtered_reverse_reads, multithread = n_threads)
# Reverse Error Plot
reverse_error_plot <-
plotErrors(error_reverse_reads, nominalQ = TRUE) +
labs(title = "Reverse Reads: Error Model")
# Look at the plots together
forward_error_plot + reverse_error_plot
# Chunk 8: infer-ASVs
# Infer ASVs on the forward sequences
dada_forward <-
dada(filtered_forward_reads,
err = error_forward_reads,
multithread = n_threads)
# Take a look at the data
# What type of data structure is it?
typeof(dada_forward) # It's a list
length(dada_forward) # How big is it? One per sample!
# What doees it look like for each sample?
dada_forward$`20210602-MA-CES1F_R1_filtered.fastq.gz`
# Reverse ASVs
dada_reverse <-
dada(filtered_reverse_reads,
err = error_reverse_reads ,
multithread = n_threads)
# Check data
dada_reverse[30]
# Chunk 9: merge-ASVs
merged_ASVs <-
mergePairs(dada_forward, filtered_forward_reads,
dada_reverse, filtered_reverse_reads,
verbose = TRUE)
# Evaluate the data output
typeof(merged_ASVs) # A list
length(merged_ASVs) # Length of the number of samples!
head(names(merged_ASVs)) # Here, we can access our current sample names
# Inspect further for each sample
#head(merged_ASVs, n = 2) # A dataframe for each sample
# We have a dataframe in each part of our list! What are in the columns?
glimpse(merged_ASVs$`20210602-MA-CEB1W_R1_filtered.fastq.gz`)
# Raw ASV
raw_ASV_table <- makeSequenceTable(merged_ASVs)
# Intuition Check: Type and dimensions of the data
dim(raw_ASV_table)
typeof(raw_ASV_table)
class(raw_ASV_table)
# write out raw_asv_table
write.table(raw_ASV_table, file = "data/01_DADA2/raw_ASV_counts.tsv",
sep = "\t", quote = FALSE, col.names = NA)
filtered_reads <-
filterAndTrim(forward_reads, filtered_forward_reads,
reverse_reads, filtered_reverse_reads,
truncLen = c(249,248), trimLeft = c(17,21),
maxN = 0, maxEE = c(1,1), truncQ = 2,
rm.phix = TRUE, compress = TRUE,
multithread = TRUE)
# Plot the 12 random samples after QC
forward_filteredQual_plot_12 <-
plotQualityProfile(filtered_forward_reads[random_samples]) +
labs(title = "Trimmed Forward Read Quality")
reverse_filteredQual_plot_12 <-
plotQualityProfile(filtered_reverse_reads[random_samples]) +
labs(title = "Trimmed Reverse Read Quality")
# Put the two plots together
forward_filteredQual_plot_12 + reverse_filteredQual_plot_12
---
title: "Assigning ASVs with DADA2"
notice more people on the server, we can modify the n_threads in this one location.
# Create vector of sample names from the filenames
sample_names <- sapply(strsplit(basename(filtered_forward_reads), "_"), `[`,1)
# Intuition Check
head(sample_names)
# Forward Reads
error_forward_reads <-
learnErrors(filtered_forward_reads, multithread = n_threads)
# Forward Error Plot
forward_error_plot <-
plotErrors(error_forward_reads, nominalQ = TRUE) +
labs(title = "Forward Reads: Error Model")
# Reverse Reads
error_reverse_reads <-
learnErrors(filtered_reverse_reads, multithread = n_threads)
# Reverse Error Plot
reverse_error_plot <-
plotErrors(error_reverse_reads, nominalQ = TRUE) +
labs(title = "Reverse Reads: Error Model")
# Look at the plots together
forward_error_plot + reverse_error_plot
# Infer ASVs on the forward sequences
dada_forward <-
dada(filtered_forward_reads,
err = error_forward_reads,
multithread = n_threads)
# Take a look at the data
# What type of data structure is it?
typeof(dada_forward) # It's a list
length(dada_forward) # How big is it? One per sample!
# What doees it look like for each sample?
dada_forward$`20210602-MA-CES1F_R1_filtered.fastq.gz`
# Reverse ASVs
dada_reverse <-
dada(filtered_reverse_reads,
err = error_reverse_reads ,
multithread = n_threads)
# Let's make a parameter to set the number of threads
n_threads = 50
# Reverse ASVs
dada_reverse <-
dada(filtered_reverse_reads,
err = error_reverse_reads ,
multithread = n_threads)
# Let's make a parameter to set the number of threads
n_threads = 35
# Reverse ASVs
dada_reverse <-
dada(filtered_reverse_reads,
err = error_reverse_reads ,
multithread = n_threads)
# What doees it look like for each sample?
dada_forward$`ERR31413861_filtered.fastq.gz`
# Check data
dada_reverse[30]
merged_ASVs <-
mergePairs(dada_forward, filtered_forward_reads,
dada_reverse, filtered_reverse_reads,
verbose = TRUE)
# Evaluate the data output
typeof(merged_ASVs) # A list
length(merged_ASVs) # Length of the number of samples!
head(names(merged_ASVs)) # Here, we can access our current sample names
# Inspect further for each sample
#head(merged_ASVs, n = 2) # A dataframe for each sample
# We have a dataframe in each part of our list! What are in the columns?
glimpse(merged_ASVs$`ERR31413861_filtered.fastq.gz`)
# Raw ASV
raw_ASV_table <- makeSequenceTable(merged_ASVs)
# Intuition Check: Type and dimensions of the data
dim(raw_ASV_table)
typeof(raw_ASV_table)
class(raw_ASV_table)
# write out raw_asv_table
write.table(raw_ASV_table, file = "data/01_DADA2/raw_ASV_counts.tsv",
sep = "\t", quote = FALSE, col.names = NA)
# Calculate summary stats
# Longest ASV?
maxLength_ASV <- max(nchar(getSequences(raw_ASV_table)))
# Calculate summary stats
# Longest ASV?
maxLength_ASV <- max(nchar(getSequences(raw_ASV_table)))
# Shortest ASV?
minLength_ASV <- min(nchar(getSequences(raw_ASV_table)))
# Mean ASV length?
meanLength_ASV <- mean(nchar(getSequences(raw_ASV_table)))
# Calculate summary stats
# Longest ASV?
maxLength_ASV <- max(nchar(getSequences(raw_ASV_table)))
# Shortest ASV?
minLength_ASV <- min(nchar(getSequences(raw_ASV_table)))
# Mean ASV length?
meanLength_ASV <- mean(nchar(getSequences(raw_ASV_table)))
# Median ASV length?
medianLength_ASV <- median(nchar(getSequences(raw_ASV_table)))
# Create a table to Inspect the distribution of sequence lengths of all ASVs in dataset
table(nchar(getSequences(raw_ASV_table)))
# Inspect the distribution of sequence lengths of all ASVs in data set
# AFTER TRIM
plot_ASVLength_raw <-
data.frame(Seq_Length = nchar(getSequences(raw_ASV_table))) %>%
ggplot(aes(x = Seq_Length )) +
geom_histogram() +
# include the x-axis scales
scale_x_continuous(limits = c(0, maxLength_ASV + 5)) +
labs(y = "Number of ASVs", x = "Raw ASV Length (bps)")
# Show the plot
plot_ASVLength_raw
r medianLength_ASV
maxLength_ASV
minLength_ASV
meanLength_ASV
medianLength_ASV
# Let's make a parameter to set the number of threads
n_threads = 20
